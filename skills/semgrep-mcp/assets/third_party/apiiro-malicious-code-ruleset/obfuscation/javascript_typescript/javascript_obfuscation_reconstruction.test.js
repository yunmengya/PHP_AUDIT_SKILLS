// TP
['a', 'b', 1, 2, 3, 4, '5'].join();

let a = ['a', 1, 'v', 0x42, 0b1, 'א'];
let b = a.join();

let c = [0x72, 101, \u0108, 108, 111].map(c => String.fromCharCode(c)).join('');  


// FP
var dd = "Hel" + asd() + 'u';
['a', 'b', dd, 'v'].join();
[1, 2, 3].map(x => x * 2);
